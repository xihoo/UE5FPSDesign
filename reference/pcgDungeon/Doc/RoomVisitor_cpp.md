# RoomVisitor.cpp

该文件包含了 `IRoomVisitor` 接口的实现。

## 主要功能

- **接口实现**：该文件为空，仅包含 `#include "RoomVisitor.h"`，表明 `IRoomVisitor` 接口的所有方法都是纯虚函数，需要在派生类中实现。

## 详细实现

- 该文件没有具体的实现代码，仅用于包含头文件。`IRoomVisitor` 接口的实现应该在 `URoomVisibilityComponent` 类或其他派生类中完成。

这个文件的存在主要是为了满足 Unreal Engine 的编译要求，确保接口的声明和定义能够被正确地包含和链接。