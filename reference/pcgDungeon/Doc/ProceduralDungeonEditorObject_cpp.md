# ProceduralDungeonEditorObject.cpp

## 概述

`ProceduralDungeonEditorObject.cpp` 是 `UProceduralDungeonEditorObject` 类的实现文件。由于 `UProceduralDungeonEditorObject` 类非常简单，只包含一个 `UPROPERTY`，因此其实现文件几乎是空的，仅包含了头文件的包含。

## 依赖项

- `ProceduralDungeonEditorObject.h`: 类的声明。